# Escopo

A decisão cobre somente o frontend interativo. O backend FastAPI, o banco PostgreSQL, a autenticação, as APIs, os raspadores, as páginas públicas e a identidade visual permanecem fora do escopo da primeira fase.
